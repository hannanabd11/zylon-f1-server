services:
  - type: web
    name: zylon-f1-server
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn f1_live_server:app --timeout 120 --workers 1 --threads 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
